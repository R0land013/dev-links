import type { PaaSLink } from ".";


const RenderDescription =
`
The fastest way to host all. Render is a unified cloud to build and
run all your apps and websites with TLS certificates, a global CDN,
DDos protection, private networks, and autodeploys from Git.
`;

const Render: PaaSLink = {
    name: 'Render',
    category: 'PaaS',
    linkUrl: 'https://render.com/',
    description: RenderDescription,
};

export default Render;