import type { PaaSLink } from ".";


const CyclicDescription =
`
Connect your GitHub repo.
We will build, deploy and manage the hosting.
`;

const Cyclic: PaaSLink = {
    name: 'Cyclic',
    category: 'PaaS',
    linkUrl: 'https://cyclic.sh/',
    description: CyclicDescription,
};

export default Cyclic;